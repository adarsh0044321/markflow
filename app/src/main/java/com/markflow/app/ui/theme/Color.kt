package com.markflow.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * MarkFlow color palette — professional, clean, and teacher-friendly.
 * Inspired by CamScanner/Adobe Scan aesthetic with a deep green primary.
 */

// ── Primary ──
val MarkFlowGreen = Color(0xFF0F5132)        // Deep forest green primary
val MarkFlowGreenDark = Color(0xFF08341E)    // Dark forest green
val MarkFlowGreenLight = Color(0xFF198754)   // Active green
val MarkFlowGreenSurface = Color(0xFFF0FDF4) // Mint-tint light container surface
val MarkFlowGreenOnSurface = Color(0xFF14532D) // Deep forest green text

// ── Secondary ──
val MarkFlowTeal = Color(0xFF0F766E)         // Slate teal secondary
val MarkFlowTealLight = Color(0xFFCCFBF1)    // Light teal container

// ── Surfaces ──
val SurfaceWhite = Color(0xFFF8FAFC)         // Slate 50 background
val SurfacePure = Color(0xFFFFFFFF)          // Pure white
val SurfaceGray = Color(0xFFF1F5F9)          // Slate 100 for card backgrounds/fills
val SurfaceDark = Color(0xFF0F172A)          // Slate 900 for dark mode background
val SurfaceDarkElevated = Color(0xFF1E293B)  // Slate 800 for dark mode cards

// ── Text ──
val TextPrimary = Color(0xFF0F172A)          // Slate 900 primary text
val TextSecondary = Color(0xFF475569)        // Slate 600 secondary text
val TextTertiary = Color(0xFF94A3B8)         // Slate 400 helper/disabled text
val TextOnDark = Color(0xFFF8FAFC)           // Slate 50 white-ish text
val TextOnGreen = Color(0xFFFFFFFF)

// ── Confidence Levels ──
val ConfidenceHigh = Color(0xFF16A34A)       // Calibrated green (Emerald-600)
val ConfidenceMedium = Color(0xFFEA580C)     // Calibrated orange (Orange-600)
val ConfidenceLow = Color(0xFFDC2626)        // Calibrated red (Red-600)

// ── Status ──
val StatusSuccess = Color(0xFF16A34A)
val StatusWarning = Color(0xFFD97706)        // Amber-600
val StatusError = Color(0xFFDC2626)
val StatusInfo = Color(0xFF2563EB)           // Blue-600

// ── Scan Overlay ──
val OverlayConfirmed = Color(0xFF22C55E)     // Green-500
val OverlayReviewing = Color(0xFFF59E0B)     // Amber-500
val OverlayLowConfidence = Color(0xFFEF4444) // Red-500
val OverlayBackground = Color(0x73000000)    // 45% black overlay

// ── Dividers & Borders ──
val DividerColor = Color(0xFFE2E8F0)         // Slate 200 divider
val BorderLight = Color(0xFFF1F5F9)          // Slate 100 border

// ── Marks Feed ──
val MarkChipBackground = Color(0xFFF0FDF4)
val MarkChipText = Color(0xFF14532D)

// ── Dark Theme ──
val DarkBackground = Color(0xFF090D16)       // Rich dark neutral background
val DarkSurface = Color(0xFF0F172A)          // Slate 900 surface
val DarkSurfaceElevated = Color(0xFF1E293B)  // Slate 800 elevated surface
val DarkGreen = Color(0xFF4ADE80)            // Light green primary for dark theme
val DarkDivider = Color(0xFF334155)          // Slate 700 divider

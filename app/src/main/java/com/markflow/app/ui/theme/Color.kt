package com.markflow.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * MarkFlow color palette — professional, clean, and teacher-friendly.
 * Inspired by CamScanner/Adobe Scan aesthetic with a deep green primary.
 */

// ── Primary ──
val MarkFlowGreen = Color(0xFF1B7A3D)
val MarkFlowGreenDark = Color(0xFF145C2E)
val MarkFlowGreenLight = Color(0xFF4CAF50)
val MarkFlowGreenSurface = Color(0xFFE8F5E9)
val MarkFlowGreenOnSurface = Color(0xFF1B5E20)

// ── Secondary ──
val MarkFlowTeal = Color(0xFF00897B)
val MarkFlowTealLight = Color(0xFFB2DFDB)

// ── Surfaces ──
val SurfaceWhite = Color(0xFFFAFAFA)
val SurfacePure = Color(0xFFFFFFFF)
val SurfaceGray = Color(0xFFF5F5F5)
val SurfaceDark = Color(0xFF1A1C1E)
val SurfaceDarkElevated = Color(0xFF2C2E30)

// ── Text ──
val TextPrimary = Color(0xFF1A1C1E)
val TextSecondary = Color(0xFF5F6368)
val TextTertiary = Color(0xFF9AA0A6)
val TextOnDark = Color(0xFFE8EAED)
val TextOnGreen = Color(0xFFFFFFFF)

// ── Confidence Levels ──
val ConfidenceHigh = Color(0xFF2E7D32)       // Green — high confidence
val ConfidenceMedium = Color(0xFFE65100)     // Deep orange — medium
val ConfidenceLow = Color(0xFFC62828)        // Red — low confidence

// ── Status ──
val StatusSuccess = Color(0xFF2E7D32)
val StatusWarning = Color(0xFFF57F17)
val StatusError = Color(0xFFC62828)
val StatusInfo = Color(0xFF1565C0)

// ── Scan Overlay ──
val OverlayConfirmed = Color(0xFF4CAF50)     // Green bounding box
val OverlayReviewing = Color(0xFFFFC107)     // Yellow bounding box
val OverlayLowConfidence = Color(0xFFF44336) // Red bounding box
val OverlayBackground = Color(0x80000000)    // Semi-transparent black

// ── Dividers & Borders ──
val DividerColor = Color(0xFFE0E0E0)
val BorderLight = Color(0xFFEEEEEE)

// ── Marks Feed ──
val MarkChipBackground = Color(0xFFE8F5E9)
val MarkChipText = Color(0xFF1B5E20)

// ── Dark Theme ──
val DarkBackground = Color(0xFF121212)
val DarkSurface = Color(0xFF1E1E1E)
val DarkSurfaceElevated = Color(0xFF2D2D2D)
val DarkGreen = Color(0xFF66BB6A)
val DarkDivider = Color(0xFF3D3D3D)
